const Button = () => {
  return (
    <button
      style={{
        backgroundColor: "#4CAF50",
        color: "#fff",
        padding: "0.5rem 1rem",
        border: "none",
        borderRadius: "4px",
        cursor: "pointer",
      }}
    >
      View Product
    </button>
  );
};

export default Button;
